#!/bin/sh

# Dev tools
apt-get update
apt-get install subversion git-core git mercurial
apt-get install build-essential libncurses5-dev zlib1g-dev gawk flex gawk gcc-multilib flex gettext

# OpenWRT buildroot installation
